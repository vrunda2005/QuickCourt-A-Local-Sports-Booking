const Home = () => {
    return (
        <div>
            <h2>Welcome to the Home Page</h2>
            <p>This is a sample app using Clerk + Cloudinary + MongoDB.</p>
        </div>
    );
};

export default Home;
